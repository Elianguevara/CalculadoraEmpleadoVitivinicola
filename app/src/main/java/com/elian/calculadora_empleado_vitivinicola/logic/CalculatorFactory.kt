package com.elian.calculadora_empleado_vitivinicola.logic

import com.elian.calculadora_empleado_vitivinicola.model.Convenio

object CalculatorFactory {
    // This class is no longer strictly needed with the current ViewModel implementation,
    // but we can keep it if we want to abstract the calculator creation.
    // However, BodegaCalculator and VinaCalculator now have different input types.
}
